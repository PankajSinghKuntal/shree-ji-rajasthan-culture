import { Product } from './types';

/**
 * The static products list is now empty as per user request.
 * All products shown on the website will be those added by the owner.
 */
export const PRODUCTS: Product[] = [];
